import pygame
from Measurements import Measurements
from pygame.locals import (
    K_UP,
    K_DOWN,
    K_LEFT,
    K_RIGHT,
    K_ESCAPE,
    KEYDOWN,
    QUIT,
)

pygame.init()

# Define constants for the screen width and height
SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600

pygame.display.set_mode((800,600))

# my stuff

given_info = dict


def input_given(event):
    if valid_measurement in my_measurement:

def no_input_given():
    pass
